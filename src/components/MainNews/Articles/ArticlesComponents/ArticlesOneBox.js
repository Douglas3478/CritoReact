import React from 'react'

const ArticlesOneBox = ({title, description}) => {
  return (
    <article>
        <h4>{title}</h4>
        <p>{description}</p>
    </article>
    
  )
}

export default ArticlesOneBox